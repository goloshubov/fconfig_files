#!/bin/bash

# activate python venv for ansible-lint
# solves non interative shell issues (e.g. pre-commit, Zed editor)

venv() {
  if [ "$VIRTUAL_ENV" != "$1" ]; then 
    source ~/python-venv/$1/bin/activate
  fi
}

venv ansible
ansible-lint $@
